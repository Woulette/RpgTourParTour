// Intégration du métier d'alchimiste dans une scène Phaser.
// Pour l'instant, ce fichier expose seulement une fonction vide qui
// servira de point d'entrée quand l'alchimie sera branchée à l'UI.

/**
 * Prépare les éléments d'alchimie pour une scène de jeu.
 *
 * @param {Phaser.Scene} scene
 * @param {object} player
 */
export function setupAlchimiste(scene, player) {
  if (!scene || !player) return;
  // À compléter plus tard (ouverture d'un atelier, UI de craft, etc.).
}

